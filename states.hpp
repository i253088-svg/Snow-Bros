#include "pre_compiled.hpp"

void player_selection(sf::RenderWindow& window, sf::Font& gaming_font, sf::Vector2u& screen_size, int& selection, int& l_select);
void level_selection(sf::RenderWindow& window, sf::Font& gaming_font, sf::Vector2u& screen_size, int& p_select, int& l_select);
void main_gameplay(sf::RenderWindow& window, sf::Font& gaming_font, sf::Vector2u& screen_size, int& p_select, int& l_select);